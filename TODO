
#######################################################################
# Copyright (c) 2026, Albert H. Stone, III <ahs3@ahs3.net>
# SPDX-License-Identifier: GPL-3.0-or-later
# SPDX-FileCopyrightText: 2025 Albert H. Stone, III <ahs3@ahs3.net>
#######################################################################

TODO
====
-- Add config options to ask or not ask for confirmation when removing
   albums, photos or attributes; this implies setting defaults, dialog
   boxes that ask whether or not to keep asking, and changing the config
   items when needed.

-- Add (under Tools?) the ability to display the raw db tables read-only.
   This could be an aid in debugging issues later, as in, what are we
   actually storing/retrieving?

-- How should icons actually be stored to make a distributable package?
   Having them in ./src/plorn seems iffy.

-- When searching attributes, allow for searching the full path.  For
   example, ',' is used by default with name attributes.  Adding the
   Name "Flintstone" and adding the name "Fred" under it gives the
   full name "Flintstone, Fred" which I should be able to search for.

-- Add a config parameter that allows the user to choose the separator
   when showing full paths for attributes.  For example, ',' is used by
   default with name attributes.  Adding the Name "Flintstone" and adding
   the name "Fred" under it gives "Flintstone, Fred" when using the
   "full" name.

-- Add a "slideshow" button to albums that shows all the images in the
   album.  Would need to add delay parameter and full-screen option.

-- If a search finds something, have a button to create a new album
   from the results (i.e., a way to organize different perspectives)

-- Add an album "Export" that creates a tarball containing an ASCII
   version of the database info (e.g., in debian/control-like file
   format?  or yaml?  and including photo info, not just album), plus
   copies of all the photos in the album.  Probably would need an
   equivalent "Import" function for symmetry (need to distinguish this
   from photo import into an album).

   -- Is there then a "backup" that does the above for all albums?
   
-- When asking for directories or files, can we make the font bigger?
   Using the stock filedialog.messagebox may not be sufficient.

-- Add a "Rescan/Refresh/Update" button to albums and/or photos; i.e.,
   check that images are still there, with option to delete items that
   are no longer present.  But, don't modify anything else.

   -- Some sort of way to verify an album is still "intact" would be
      good; i.e., verify that all the photos are actually still there.


